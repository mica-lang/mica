# Tests that the string representation of dicts makes sense.

assert(string([:]) == "[:]")
assert(string([1: 2]) == "[1: 2]")
# We can't really test multiple pairs since lists do not maintain deterministic order.
# assert(string([1: 2, 3: 4]) == "[1: 2, 3: 4]")
