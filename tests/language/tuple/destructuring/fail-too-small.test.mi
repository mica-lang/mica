# Test that destructuring a tuple of a non-matching length fails.
# @error error: type mismatch, expected Tuple(2) but got Tuple(1)
# @error stack traceback (most recent call first):
# @error     {file}:{:LINE}:1  <main>

let (a, b) = (1,)  # @line LINE
