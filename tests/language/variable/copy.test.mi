# Declare a variable and copy it to another variable.

let x = 30
let y = x
assert(x == y)
# y does not contain a reference to the original variable, rather its value.
x = 40
assert(x == 40)
assert(y == 30)
